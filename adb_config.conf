# Configuration for adb_reconnect.sh script

# Your phone's fixed IP address (or the one assigned by your router)
PHONE_IP="192.168.0.102"

# Typical range for random wireless debugging ports (e.g., 30000-50000)
RANDOM_PORT_RANGE="30000-50000"
